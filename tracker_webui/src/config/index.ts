import { API } from "./apis";
import { Page, Public } from "./browser";

const LOCAL_STORAGE_KEY: string = "X-TRACKER-KEY";
const PAGE_SIZE: number = 5;

export { API, LOCAL_STORAGE_KEY, PAGE_SIZE, Page, Public };
